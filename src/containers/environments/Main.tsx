import React from 'react'

import { AppBar, Toolbar } from '@material-ui/core'

const Main: React.FC = () => {
  return (
    <React.Fragment>
      <AppBar>
        <Toolbar></Toolbar>
      </AppBar>
    </React.Fragment>
  )
}

export default Main
